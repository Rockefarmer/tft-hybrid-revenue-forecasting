[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tft-hybrid-revenue-forecasting"
version = "0.1.0"
description = "Hybrid TFT forecasting with FinBERT and Llama-3 features"
requires-python = ">=3.10"
