import pandas as pd


def build_finbert_quarterly_features(transcript_df: pd.DataFrame) -> pd.DataFrame:
    # Placeholder for transcript chunking, inference, and aggregation.
    return transcript_df.copy()
